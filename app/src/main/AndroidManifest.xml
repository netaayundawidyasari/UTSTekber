<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aplikasi.utstekber">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SyntaxBroadcastReceiverActivity"></activity>
        <activity android:name=".SyntaxContentProvidersActivity" />
        <activity android:name=".SyntaxIntentActivity" />
        <activity android:name=".SyntaxActivityActivity" />
        <activity android:name=".SyntaxActivity" />
        <activity android:name=".BroadcastReceiverActivity" />
        <activity android:name=".ContentProvidersActivity" />
        <activity android:name=".IntentActivity" />
        <activity android:name=".ActivityActivity" />
        <activity android:name=".ServiceActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>